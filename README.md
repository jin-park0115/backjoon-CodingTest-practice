fs모듈
모듈(module)이란?
여러 기능들에 관한 코드가 모여있는 하나의 파일이다.
다른 모듈을 사용할 때는 require를, 모듈을 해당 스코프 밖으로 보낼 때에는 module.exports를 사용한다.

fs모듈에서 readFileSync함수로 표준 인풋을 참조해서 불러오는 것.
불러온 입력값을 toString으로, trim()으로 \n 제거, split(" ")으로 구분해서 변수를 배열에 저장 하는 것.

## 1일차는 입출력과 사칙연산

## 2일차는 조건문

## 3일차~7일차는 반복문

3일차에 반복문 공부 + 두통이슈
4일차에 주일이라 못함
5일차 공부 후 풀이 and 복습
6일차 ing 뭔 버튼 로딩중이 활성화가 되지 않아 문제를 제출을 못하는 중

## 8일차부터 1차원배열

왜 이렇게 커밋이 빠르냐 할 수 있는데 진짜, 제 순수로 풀고 있습니다.

## 시작일 11월 12일(수요일)

## 배열 메서드

1. 원본을 바꾸는 메서드 (추가 or 삭제)
   pop() = 배열 맨 뒤에 요소 **삭제 및 반환** ❌
   push() = 배열 맨 뒤에 요소 **추가** ➕
   unshift() = 배열 맨 앞에 요소 **추가** ➕
   shift() = 배열 맨 앞 요소 **삭제** ❌
   splice(시작, 개수) = 원하는 위치의 요소를 **삭제하거나 교체** ❌, 🔄

2. 가공 해서 새 배열로 만들어 주는 메서드

- 원본은 그대로 두고, 조건에 맞게 변형된 **복사본**을 만든다.
  map() = 모든 요소를 순회하며 계산해서 새로운 배열을 만든다.
  filter() = 조건에 맞는 요소만 골라내서 새로운 배열을 만든다.
  slice(시작, 끝) = 배열의 일부분을 잘라서 가져온다.
  concat() = 두 배열을 합친다.

3. 배열 탐색 및 확인
   배열 안에 뭐가 있는지 찾을 때 사용.
   indexOf(값) = 특정 값이 몇 번째 인덱스에 있는지 찾는다 없으면 -1 반환.
   includes(값) = 특정 값이 포함되어 있는지 true/false로 알려준다.
   find() = 조건에 맞는 첫 번째 요소를 반환한다.
   findIndex() = 조건에 맞는 첫 번째 요소의 인덱스를 반환한다.

4. 배열 변형 및 정렬
   sort() = 배열을 정렬한다. 숫자 정렬 시 (a, b) = > a - b 사용 필수다.
   reverse() = 배열 순서를 거꾸로 뒤집는다.
   join("") = 배열을 문자열로 합친다.

5. 그 외
   forEach() = 그냥 배열을 한 바퀴 돈다.
   reduce() = 배열의 모든 값을 하나로 합친다.

map() 예시

```
  const numbers = [1, 2, 3, 4, 5];
  const doubled = numbers.map(num => num * 2);
  console.log(doubled); // [2, 4, 6, 8, 10]
  console.log(numbers); // [1, 2, 3, 4, 5] (원본은 그대로)
```

filter() 예시

```
  const numbers = [1, 2, 3, 4, 5];

  // 3보다 큰 숫자만 골라내기
  const biggerThanThree = numbers.filter(num => num > 3);

  console.log(biggerThanThree); // [4, 5]
```

slice() 예시

```
const fruits = ['사과', '바나나', '포도', '딸기', '수박'];
0 1 2 3 4

// 인덱스 1번부터 3번 '전'까지 (즉, 1, 2번만)
const subFruits = fruits.slice(1, 3);

console.log(subFruits); // ['바나나', '포도']
```

concat() 예시

```
const arr1 = [1, 2];
const arr2 = [3, 4];

const combined = arr1.concat(arr2);

console.log(combined); // [1, 2, 3, 4]
```
